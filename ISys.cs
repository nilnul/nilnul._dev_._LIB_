namespace nilnul.dev
{
	/// <summary>
	/// it has a scope, in that an explicit border (a boolean func to determine whether an obj/element is inside) exists;
	/// ; its outside is called environment/context
	/// ; its internal embodies an innerworking/processing/mechanism/metabolism
	/// ; input thru inlet, and output thru outlet.
	/// </summary>
	/// <remarks>
	/// ex:
	///		a procedure is not a system, as the border is not explicit.
	///	eg:
	///		a type, an obj/instance, a project/assembly, a sln /app, an operation system, a computer as a hardware device.;
	/// </remarks>
	public interface ISys {

	}


}
